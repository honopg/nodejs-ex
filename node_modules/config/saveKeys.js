var mongoose = require('mongoose');
var user = require('config/models');

exports.savekeys = function(identUser,publickey,privatekey,callback) {
	//update,token: id_user
	user.findByIdAndUpdate({"_id": identUser}, {'$set': {'publickey':publickey,/*'privatekey':privatekey*/}}, {upsert:true}, function(err, users) {
			if (err) {
				callback("User not exist, ident error!");
			}else{
				//console.log(users);
				callback("Keys saved");
				}
			
	});
	
	/*Otra forma
	user.find({"_id": identUser},function(err,users){
		if(users.length != 0){
			
			user.update( {"_id":users[0]._id}, {'$set': {'publickey':publickey,'privatekey':privatekey}}, {upsert:true}, function(err, raw){
			
			if (err) {
				callback("Eror");
			}
			//console.log(users);
			callback("Keys saved");
			});
			
		
		}else {
			callback("User not exist, ident error!");
		}
	
	});	*/
}